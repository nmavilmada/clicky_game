
import React from "react";
import "./Title.css";

const Title = props => (
    <div className="titlebox">
        <h1>Dog Clicky Game!</h1>
        <p>Memory Game : Avoid selecting the same image twice</p>
    </div>
);
export default Title;